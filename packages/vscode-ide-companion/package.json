{
  "name": "ouroboros-code-vscode-ide-companion",
  "displayName": "Ouroboros Code Companion",
  "description": "Enable Ouroboros Code with direct access to your IDE workspace.",
  "version": "1.1.0-alpha.1",
  "publisher": "google",
  "icon": "assets/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/ouroboros-ai/ouroboros-code.git",
    "directory": "packages/vscode-ide-companion"
  },
  "engines": {
    "vscode": "^1.99.0"
  },
  "license": "LICENSE",
  "preview": true,
  "categories": [
    "AI"
  ],
  "keywords": [
    "ouroboros-code",
    "ouroboros cli",
    "ouroboros",
    "ouroboros ai",
    "gemini-cli",
    "gemini",
    "cli",
    "ide integration",
    "ide companion"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "languages": [
      {
        "id": "ouroboros-diff-editable"
      }
    ],
    "commands": [
      {
        "command": "ouroboros.diff.accept",
        "title": "Ouroboros Code: Accept Diff",
        "icon": "$(check)"
      },
      {
        "command": "ouroboros.diff.cancel",
        "title": "Ouroboros Code: Close Diff Editor",
        "icon": "$(close)"
      },
      {
        "command": "ouroboros-code.runCli",
        "title": "Ouroboros Code: Run"
      },
      {
        "command": "ouroboros-code.showNotices",
        "title": "Ouroboros Code: View Third-Party Notices"
      }
    ],
    "menus": {
      "commandPalette": [
        {
          "command": "ouroboros.diff.accept",
          "when": "ouroboros.diff.isVisible"
        },
        {
          "command": "ouroboros.diff.cancel",
          "when": "ouroboros.diff.isVisible"
        }
      ],
      "editor/title": [
        {
          "command": "ouroboros.diff.accept",
          "when": "ouroboros.diff.isVisible",
          "group": "navigation"
        },
        {
          "command": "ouroboros.diff.cancel",
          "when": "ouroboros.diff.isVisible",
          "group": "navigation"
        }
      ]
    },
    "keybindings": [
      {
        "command": "ouroboros.diff.accept",
        "key": "ctrl+s",
        "when": "ouroboros.diff.isVisible"
      },
      {
        "command": "ouroboros.diff.accept",
        "key": "cmd+s",
        "when": "ouroboros.diff.isVisible"
      }
    ]
  },
  "main": "./dist/extension.cjs",
  "type": "module",
  "scripts": {
    "vscode:prepublish": "npm run generate:notices && npm run check-types && npm run lint && node esbuild.js --production",
    "build": "npm run compile",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "vsce package --no-dependencies",
    "generate:notices": "node ./scripts/generate-notices.js",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "test:ci": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/node": "20.x",
    "@types/vscode": "^1.99.0",
    "@typescript-eslint/eslint-plugin": "^8.31.1",
    "@typescript-eslint/parser": "^8.31.1",
    "esbuild": "^0.25.3",
    "eslint": "^9.25.1",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.15.1",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "fdir": "^6.4.6",
    "zod": "^3.25.76"
  }
}
